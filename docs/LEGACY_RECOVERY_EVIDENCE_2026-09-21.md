# Legacy recovery evidence — 2026-09-21

Author/Agent: CHATGPT-COORD

This checkpoint records a fresh public-search pass against unresolved legacy targets. It does not invent link targets that the search index does not expose.

## p0122 / p0121 boulder resources

Public search still exposes the legacy p0122 page and confirms:
- the project produced a boulder brochure;
- the page contains a “Скачать брошюру” anchor;
- the project produced an interactive boulder map;
- the page contains a map image/link with “Нажмите на карту для перехода”.

The search result does not expose the brochure anchor href or the final interactive-map target/dataset. Direct page open still fails in the current fetch environment because of legacy-page decoding/fetch errors.

Disposition: DEFER exact targets. Keep p0121/p0122 source records and do not synthesize replacement URLs.

## p0087 publication downloads

Public search still exposes multiple download/detail labels on the publications page, including:
- museum guide 2013 internet version;
- Russian/German Rominta tourism brochure versions;
- “Скачать книгу” links for some catalogue items.

The search result does not expose those anchor hrefs, and direct page fetch is unavailable in the current environment.

Disposition: keep bibliography and verified anchor existence; exact download URLs remain unresolved rather than guessed.

## rominten.wystynez.ru

Fresh public search did not recover primary historical subdomain pages or a reliable archived copy sufficient for source-preserving migration.

Disposition: DEFER until primary/reliable archived evidence is recoverable.

## p92 mixed media

The legacy p92 programme page is still indexed and confirms the programme/workbook context, but the available search representation does not expose enough binary visual evidence to map the 20 exact media files to photograph / Rien Poortvliet illustration / Victoria Vetiver workbook / map-scheme roles file-by-file.

Disposition: DEFER public gallery mapping; preserve exact URLs and separate credit scopes already captured.

## Rule

A search-index snippet proving that an anchor or resource existed is not proof of its exact href. Exact download/map targets must remain null/pending until the target itself is recovered.
