(()=>{'use strict';
const API='https://qwiecwfltybytqwytqoy.supabase.co/functions/v1',KEY='vishtynets_audio_entitlement_v2',RETURN_PARAM='audio_payment';
const sleep=ms=>new Promise(r=>setTimeout(r,ms));
const set=(k,v)=>{try{localStorage.setItem(k,JSON.stringify(v))}catch{}};
async function createPayment(){const returnUrl=new URL(location.href);returnUrl.searchParams.delete(RETURN_PARAM);const r=await fetch(`${API}/audio-guide-payment`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({action:'create',amount:'100.00',description:'Аудиогид Виштынецкого экомузея — доступ 24 часа',return_url:returnUrl.toString()})});const d=await r.json().catch(()=>({}));if(!r.ok||!d.confirmation_url)throw new Error(d.error||'payment_create_failed');location.href=d.confirmation_url;}
async function finishPayment(){const ref=new URL(location.href).searchParams.get(RETURN_PARAM);if(!ref)return;for(let i=0;i<12;i++){try{const r=await fetch(`${API}/audio-guide-payment-status?audio_payment=${encodeURIComponent(ref)}`,{cache:'no-store'});const d=await r.json();if(d.status==='succeeded'&&d.active&&d.expires_at){const now=Date.now(),expires=new Date(d.expires_at).getTime();set(KEY,{state:'active',activatedAt:now,startsAt:now,expiresAt:expires,source:'yookassa',paymentId:d.payment_id||null});const clean=new URL(location.href);clean.searchParams.delete(RETURN_PARAM);history.replaceState({},'',clean.toString());location.reload();return;}}catch(e){}await sleep(2500)}alert('Платёж принят, но подтверждение ещё не получено. Обновите страницу через несколько секунд.')}
function wire(){const modal=document.querySelector('.audio-access-modal');if(!modal)return false;const pay=modal.querySelector('.audio-access-future button');if(pay&&!pay.dataset.yookassa){pay.disabled=false;pay.textContent='Оплатить 100 ₽';pay.dataset.yookassa='1';pay.addEventListener('click',async()=>{pay.disabled=true;pay.textContent='Переход к оплате…';try{await createPayment()}catch(e){pay.disabled=false;pay.textContent='Оплатить 100 ₽';alert('Не удалось создать платёж. Попробуйте ещё раз.')}})}const small=modal.querySelector('small');if(small){small.innerHTML='Без регистрации. Доступ к платным дорожкам действует 24 часа.<br><a href="payment-terms.html" target="_blank" rel="noopener">Условия оплаты и оферта</a>'}return true}
async function init(){await finishPayment();for(let i=0;i<30;i++){if(wire())break;await sleep(250)}}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init,{once:true});else init();
})();
