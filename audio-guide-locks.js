(()=>{'use strict';
const init=()=>{const screen=document.querySelector('.screen-audio');const host=document.querySelector('#audioGuideTracks');const guide=window.VISHTYNETS_AUDIO_GUIDES?.museum;if(!screen||!host||!guide)return;const tracks=Array.isArray(guide.tracks)?guide.tracks:[];const paid=new Map(tracks.filter(t=>t.access==='paid').map(t=>[String(t.id),t]));if(!paid.size)return;const key='vishtynets_audio_entitlement_v2';const active=()=>{try{const e=JSON.parse(localStorage.getItem(key)||'null');return e&&(e.state==='pending'||(e.state==='active'&&Number(e.expiresAt)>Date.now()))}catch(_){return false}};const lock='<svg viewBox="0 0 24 24" aria-hidden="true"><rect x="5" y="10" width="14" height="10" rx="2"/><path d="M8 10V7a4 4 0 0 1 8 0v3" fill="none"/></svg>';const apply=()=>{const unlocked=active();host.querySelectorAll('.audio-guide-track-card-v2').forEach(card=>{const t=paid.get(String(card.dataset.trackId));if(!t)return;card.classList.toggle('is-locked',!unlocked);const b=card.querySelector('.audio-guide-track-play');const r=card.querySelector('.audio-guide-track-read');if(!unlocked){if(b){b.innerHTML=lock;b.setAttribute('aria-label','Доступ закрыт')}if(r)r.setAttribute('aria-label','Доступ закрыт')}})};new MutationObserver(apply).observe(host,{childList:true,subtree:true});apply()};if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init,{once:true});else init()})();